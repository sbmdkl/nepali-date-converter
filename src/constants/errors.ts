enum Errors {
	INVALID_DAY = 'Day is invalid.',
	INVALID_YEAR = 'Year is invalid.',
	INVALID_MONTH = 'Month is invalid.',
	BS_DATE_OUT_OF_RANGE = 'BS Date out of range',
	AD_DATE_OUT_OF_RANGE = 'AD Date out of range',
	DATE_FORMAT_INVALID = 'Date format is invalid',
	DATE_GREATER_THAN_TODAY = 'Date cannot be greater then today',
}

export default Errors;
